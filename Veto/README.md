# Veto Diagnostics Folder

On the broadcast PC this folder contains browser crash diagnostics, not the
scoreboard application. The diagnostic files are deliberately excluded because
they are machine-generated and may contain private runtime information.

The active application is in `../Veto OTT`. Older source versions are in
`../Veto Live`, `../old` and `../older`.
